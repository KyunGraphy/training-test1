const img = require('../model/image ')
const b64 = require('../service/imgtoB64')

module.exports = {
    upload: async (req, res) => {
        let {
            avaLink, projectLink
        } = req.body
        
    }
}